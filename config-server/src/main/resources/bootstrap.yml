server:
  port: 8888
spring:
  config:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KrzysztofSuc/creditapp-microservices-config
#    bus:
#      enabled: true
#  rabbitmq:
#    hostname: localhost
#    port: 5672
#    username: guest
#    password: guest
